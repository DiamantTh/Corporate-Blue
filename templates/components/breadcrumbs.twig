<nav class="cb-breadcrumb" aria-label="breadcrumbs">
    <ol>
        {% for item in _app.breadcrumbs %}
            <li {{ item.url is empty ? 'aria-disabled="true"' }}>
                <a href="{{ item.url }}">{{ item.label }}</a>
            </li>
        {% endfor %}
    </ol>
</nav>